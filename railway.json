{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "php artisan migrate --force && php artisan key:generate && php artisan config:clear && php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}